import { RandomColorCardDirective } from './random-color-card.directive';

describe('RandomColorCardDirective', () => {
  it('should create an instance', () => {
    const directive = new RandomColorCardDirective();
    expect(directive).toBeTruthy();
  });
});
