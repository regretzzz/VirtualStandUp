import { FloatAddButtonDirective } from './float-add-button.directive';

describe('FloatAddButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new FloatAddButtonDirective();
    expect(directive).toBeTruthy();
  });
});
